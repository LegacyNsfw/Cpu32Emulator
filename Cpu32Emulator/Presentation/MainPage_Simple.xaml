<Page x:Class="Cpu32Emulator.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Cpu32Emulator.Presentation"
      NavigationCacheMode="Required">

  <Page.DataContext>
    <local:MainViewModel />
  </Page.DataContext>

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Title Bar -->
    <Border Grid.Row="0" 
            Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
            Padding="20,10">
      <TextBlock Text="CPU32 Emulator - Phase 3 UI"
                 FontSize="24"
                 FontWeight="Bold"
                 HorizontalAlignment="Center" />
    </Border>

    <!-- Main Content -->
    <Grid Grid.Row="1" Margin="20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <!-- Control Panel -->
      <StackPanel Grid.Row="0" 
                  Orientation="Horizontal" 
                  Spacing="10"
                  Margin="0,0,0,20">
        <Button Content="Load Program" 
                Command="{Binding LoadProgramCommand}"
                Style="{StaticResource AccentButtonStyle}" />
        <Button Content="Run" 
                Command="{Binding RunCommand}" />
        <Button Content="Step" 
                Command="{Binding StepCommand}" />
        <Button Content="Stop" 
                Command="{Binding StopCommand}" />
        <Button Content="Reset" 
                Command="{Binding ResetCommand}" />
      </StackPanel>

      <!-- Content Tabs -->
      <TabView Grid.Row="1">
        <TabViewItem Header="Disassembly">
          <Border BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                  BorderThickness="1">
            <ScrollViewer>
              <TextBlock Text="{Binding DisassemblyText}"
                        FontFamily="Consolas, Courier New"
                        Padding="10"
                        TextWrapping="NoWrap" />
            </ScrollViewer>
          </Border>
        </TabViewItem>

        <TabViewItem Header="Registers">
          <Border BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                  BorderThickness="1">
            <ScrollViewer>
              <TextBlock Text="{Binding RegistersText}"
                        FontFamily="Consolas, Courier New"
                        Padding="10"
                        TextWrapping="NoWrap" />
            </ScrollViewer>
          </Border>
        </TabViewItem>

        <TabViewItem Header="Memory">
          <Border BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                  BorderThickness="1">
            <ScrollViewer>
              <TextBlock Text="{Binding MemoryText}"
                        FontFamily="Consolas, Courier New"
                        Padding="10"
                        TextWrapping="NoWrap" />
            </ScrollViewer>
          </Border>
        </TabViewItem>
      </TabView>
    </Grid>

    <!-- Status Bar -->
    <Border Grid.Row="2" 
            Background="{ThemeResource SystemControlBackgroundAltMediumBrush}"
            BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
            BorderThickness="0,1,0,0"
            Padding="20,5">
      <TextBlock Text="{Binding StatusMessage}" />
    </Border>
  </Grid>
</Page>
